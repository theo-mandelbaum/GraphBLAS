Scaling benchmark results
========================

This folder stores the thread-scaling benchmark output.

Files
-----
  scaling_detailed.csv       - CSV with matrix size, thread count, time, speedup, efficiency

Usage
-----
Run `../demonstrate_scaling.sh` from `GraphBLAS/profiling`.

Interpretation
--------------
Use the CSV to verify how runtime changes across thread counts and matrix sizes.
